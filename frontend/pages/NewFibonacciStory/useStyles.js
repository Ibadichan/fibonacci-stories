import { makeStyles } from '@material-ui/core';

const useStyles = makeStyles({
  Paper: {
    padding: '2rem',
    marginBottom: '2rem',
  },
  TextField: {
    marginBottom: '2rem',
  },
  FibonacciNumber: {
    marginTop: '2rem',
    wordWrap: 'break-word',
  },
});

export default useStyles;
